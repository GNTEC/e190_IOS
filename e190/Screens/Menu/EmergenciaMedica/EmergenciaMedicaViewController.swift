//
//  EmergenciaMedicaViewController.swift
//  e190
//
//  Created by Ivan on 18/07/17.
//  Copyright © 2017 GNTEC. All rights reserved.
//

import UIKit

class EmergenciaMedicaViewController: UIViewController {

    
    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    
    /*
    @IBAction func btn_salvar(_ sender: Any) {
        
        if txt_peso.text == nil || txt_peso.text == "" {
        
            let alert = UIAlertController(title: "Informação", message: "Favor Preencher seu peso", preferredStyle: UIAlertControllerStyle.alert)
        
            // add an action (button)
            alert.addAction(UIAlertAction(title: "OK", style: UIAlertActionStyle.default, handler: nil))
        
            // show the alert
            self.present(alert, animated: true, completion: nil)
        
            return
        }
        
        if txt_altura.text == nil || txt_peso.text == "" {
            
            let alert = UIAlertController(title: "Informação", message: "Favor Preencher sua altura", preferredStyle: UIAlertControllerStyle.alert)
            
            // add an action (button)
            alert.addAction(UIAlertAction(title: "OK", style: UIAlertActionStyle.default, handler: nil))
            
            // show the alert
            self.present(alert, animated: true, completion: nil)
            
            return
        }

    }
   */

}
